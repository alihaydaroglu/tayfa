# {{TITLE}}

_(One-paragraph description of what this workstream covers and why it
exists. Replace this placeholder.)_

Every task has an owner tag. Address another agent by writing their
tag in the task body. Keep entries terse — link out to specs or
scratchboards for detail. When a task is finished it moves to `## Done`
with its tag rewritten from `@tag` → `#tag`.

See [AGENTS.md](AGENTS.md) for the global agent registry.

Agents active in this stream: _(fill in)_

---

## Current Sprint

**Goal:** _(one sentence — what does "done" look like for this sprint?)_

**Status YYYY-MM-DD:** _(short status line, updated as the sprint moves.)_

### Tasks — @<tag>

1. [ ] **Task title.** Body of the task. Link out to specs as
       needed. Address other agents by tag (`@other`) inline if you
       need their input.

---

## Memos

Cross-agent notes. Address with `**To:** @<tag>`. When the addressed
agent has handled the memo, they flip their tag to `#`. Once every
addressed agent has acknowledged, strike the memo header with `~~`.

---

### Example memo title

**Author:** @<author>
**To:** @<recipient>

Body of the memo. Be specific about what you're asking for.

Flip `@<recipient>` → `#<recipient>` once you've handled this.

---

## Contracts / Shared Shapes

Tables of stable interfaces this workstream owns or consumes.

| Contract | Summary | Spec |
|----------|---------|------|

---

## Done

Closed tasks live here, tags flipped to `#`. Keep dated entries so
future readers can see the sequence.

---

## Change Log

Reverse-chron, one line per material change: `YYYY-MM-DD @tag: what changed`.

- YYYY-MM-DD @<tag>: Workstream created.
